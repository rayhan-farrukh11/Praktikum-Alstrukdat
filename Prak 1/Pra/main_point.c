#include "point.c"
#include <stdio.h>
#include <assert.h>

int main() {
    POINT p = {2,1};
    // Absis(p) = 2;
    // BacaPOINT(&p);
    // TulisPOINT(p);
    // TulisPOINT(PlusDelta(p,2,5));
    // TulisPOINT(MirrorOf(p,false));
    return 0;
}